# Titip Kilat Web Client

Flutter Web client with red/orange branding and role-based routing for USER, COURIER, TRAVELER, ADMIN, and OPERATOR. The API base URL for local development is `http://localhost:8000/api`.

Run with `flutter pub get` then `flutter run -d chrome --web-port 8080`.
